# YouTube Video Agent - Requirements

flask>=3.0.0
apscheduler>=3.10.0
anthropic>=0.40.0
python-dotenv>=1.0.0
soundfile>=0.12.1
numpy>=1.24.0

# Video generation (install separately - large models)
# diffusers>=0.32.0
# transformers>=4.45.0
# accelerate>=0.33.0
# torch>=2.1.0  (install with CUDA: pip install torch --index-url https://download.pytorch.org/whl/cu121)

# Audio generation
# kokoro>=0.9.2  (pip install kokoro)
# audiocraft  (pip install audiocraft)
